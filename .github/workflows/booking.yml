name: Auto Booking Worksite

on:
  schedule:
    # Berjalan setiap hari Jumat jam 08:00 WIB (01:00 UTC)
    - cron: '0 1 * * 5'
  workflow_dispatch: # Tombol jalankan manual via GitHub Actions UI

jobs:
  run-booking:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openpyxl playwright

      - name: Install Playwright Browsers
        run: |
          playwright install chromium
          playwright install-deps

      - name: Run Booking Script
        run: python booking.py # Perbaikan nama file di sini

      - name: Upload Single Video Session Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: full-booking-video-session
          path: videos/
          retention-days: 7
